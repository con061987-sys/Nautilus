"""src.tests — top-level integration tests.

These are the cross-bridge, end-to-end tests that prove the whole
pipeline works. Per-bridge unit tests live inside each bridge's
own tests/ directory.
"""
